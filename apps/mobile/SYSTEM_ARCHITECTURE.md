# 시스템 아키텍처 개요 (MVP Mode)

## 🏗️ 전체 아키텍처

```
┌─────────────────────────────────────────────────────────────────┐
│                         FLUTTER FRONTEND                         │
│                   (40+ Pages, 5 BLoCs)                          │
└─────────────────────────────────────────────────────────────────┘
                                │
                    ┌───────────┴───────────┐
                    ▼                       ▼
        ┌──────────────────────┐  ┌──────────────────────┐
        │   GoRouter (30 MVP   │  │  GoRouter (150+      │
        │   Routes)            │  │   Full Features)     │
        │  MVP 모드 활성화     │  │  개발/테스트용      │
        └──────────────────────┘  └──────────────────────┘
                    │                       │
        ┌───────────┴───────────────────────┴──────────┐
        ▼                                               ▼
┌──────────────────────────────────┐    ┌──────────────────────────┐
│   SERVICE LOCATOR (DI)           │    │   MVPConfig              │
│  ┌──────────────────────────┐   │    │  ┌────────────────────┐  │
│  │ • OfflineModeManager     │   │    │  │ • enableMVPMode    │  │
│  │ • AnalyticsManager       │   │    │  │ • enabledFeatures  │  │
│  │ • HttpClientService      │   │    │  │ • BackendConfig    │  │
│  │ • HomeRepository         │   │    │  │ • HardwareConfig   │  │
│  │ • MeasurementRepository  │   │    │  │ • AIConfig         │  │
│  │ • DataHubRepository      │   │    │  │ • Performance      │  │
│  │ • ErrorTracker           │   │    │  │ • OfflineSync      │  │
│  │ • PerformanceMonitor     │   │    │  │ • ABTesting        │  │
│  └──────────────────────────┘   │    │  └────────────────────┘  │
└──────────────────────────────────┘    └──────────────────────────┘
        │                                         │
        └─────────────────┬─────────────────────┘
                          ▼
        ┌─────────────────────────────────────┐
        │    HTTP OPTIMIZATION LAYER          │
        │  ┌────────────────────────────────┐ │
        │  │ CachingInterceptor             │ │
        │  │  • 1시간 최대 나이             │ │
        │  │  • 7일 stale-while-revalidate │ │
        │  │  • LRU 100개 항목 제한        │ │
        │  └────────────────────────────────┘ │
        │  ┌────────────────────────────────┐ │
        │  │ PerformanceMonitorInterceptor  │ │
        │  │  • < 200ms 목표 모니터링       │ │
        │  │  • p95, p99 추적              │ │
        │  └────────────────────────────────┘ │
        └─────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │    OFFLINE-FIRST LAYER              │
        │  ┌────────────────────────────────┐ │
        │  │ OfflineModeManager             │ │
        │  │  • Hive 로컬 저장소            │ │
        │  │  • 자동 동기화 큐              │ │
        │  │  • 충돌 해결 (로컬-우선)      │ │
        │  │  • 배경 동기화 (5분 마다)    │ │
        │  └────────────────────────────────┘ │
        │  ┌────────────────────────────────┐ │
        │  │ Hive Boxes                     │ │
        │  │  • offline_data (캐시)         │ │
        │  │  • sync_queue (대기)           │ │
        │  │  • metadata (메타정보)         │ │
        │  └────────────────────────────────┘ │
        └─────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │    ANALYTICS & MONITORING           │
        │  ┌────────────────────────────────┐ │
        │  │ AnalyticsManager               │ │
        │  │  • Firebase Analytics          │ │
        │  │  • Remote Config (A/B 테스트)  │ │
        │  │  • 사용자 세그멘테이션         │ │
        │  └────────────────────────────────┘ │
        │  ┌────────────────────────────────┐ │
        │  │ ErrorTracker                   │ │
        │  │  • 측정 오류                   │ │
        │  │  • 동기화 오류                 │ │
        │  │  • API 오류                    │ │
        │  └────────────────────────────────┘ │
        └─────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │    MOCK/REAL BACKEND (MVP)          │
        │  ┌────────────────────────────────┐ │
        │  │ 3 Microservices                │ │
        │  │  • auth-service (Go)           │ │
        │  │  • measurement-service (Node)  │ │
        │  │  • ai-service (Python)         │ │
        │  │                                 │ │
        │  │ 주의: 현재 Mock 필요            │ │
        │  │      (Firebase Emulator)        │ │
        │  └────────────────────────────────┘ │
        └─────────────────────────────────────┘
                          │
                          ▼
        ┌─────────────────────────────────────┐
        │    HARDWARE LAYER (Simulator)       │
        │  ┌────────────────────────────────┐ │
        │  │ HardwareSimulator              │ │
        │  │  • NFC 모킹 (cartridge ID)     │ │
        │  │  • BLE 모킹 (1초 연결)         │ │
        │  │  • ADC 모킹 (24-bit)           │ │
        │  │  • 측정 데이터 생성            │ │
        │  └────────────────────────────────┘ │
        │  ┌────────────────────────────────┐ │
        │  │ Rule-Based Coaching            │ │
        │  │  (AI는 Phase 2에서)            │ │
        │  └────────────────────────────────┘ │
        └─────────────────────────────────────┘
```

---

## 📦 핵심 컴포넌트 상세

### 1. MVP 설정 시스템
```dart
MVPConfig
├── enableMVPMode: true/false
├── enabledFeatures
│   ├── home: true
│   ├── measurement: true
│   ├── dataHub: true
│   ├── auth: true
│   ├── coaching: true (규칙 기반)
│   ├── marketplace: false (Phase 2)
│   ├── telemedicine: false (Phase 2)
│   └── community: false (Phase 2)
├── backendConfig
│   ├── useMockBackend: true (개발)
│   ├── enabledServices: 3 (auth, measurement, ai)
│   └── apiBaseUrl: "http://localhost:3000"
├── hardwareConfig
│   ├── simulateNFC: true
│   ├── simulateBLE: true
│   ├── simulateADC: true
│   └── generateRealisticData: true
├── aiConfig
│   ├── useRuleBasedCoaching: true
│   ├── useMLPrediction: false
│   └── cacheCoachingHours: 24
└── performanceConfig
    ├── enableCaching: true
    ├── maxApiResponseMs: 200
    ├── cacheMaxAgeSeconds: 3600
    └── cacheMaxStaleSeconds: 604800
```

### 2. 오프라인 동기화 흐름
```
┌─────────────────────────────────────────┐
│          데이터 저장 요청                  │
└────────────────┬────────────────────────┘
                 │
         ┌───────▼────────┐
         │  온라인 상태?   │
         └───────┬────────┘
                 │
        ┌────────┴────────┐
        │                 │
    YES │                 │ NO
        ▼                 ▼
    ┌─────────┐      ┌─────────┐
    │ API에   │      │ Hive에  │
    │ 저장    │      │ 저장    │
    └────┬────┘      └────┬────┘
         │                │
         ▼                ▼
    ┌─────────┐      ┌──────────────┐
    │ 성공    │      │ Sync Queue에 │
    │ → Hive │      │ 대기         │
    │ 캐시   │      └──────┬───────┘
    └─────────┘             │
                        재연결 감지
                            │
                            ▼
                    ┌──────────────┐
                    │ 배치 동기화   │
                    │ (100개 항목)  │
                    └──────┬───────┘
                            │
                    ┌───────▼────────┐
                    │ 성공?          │
                    └───────┬────────┘
                            │
                ┌───────────┴────────────┐
                │                        │
            YES │                        │ NO
                ▼                        ▼
            ┌────────┐             ┌──────────┐
            │ Queue  │             │ 재시도   │
            │ 정리   │             │ (exp bo) │
            └────────┘             └──────────┘
```

### 3. 성능 모니터링
```
API Request → CachingInterceptor
              ├─ 캐시 확인 (< 1ms) → Response
              │
              └─ 캐시 미스 → Network Request
                          ├─ PerformanceMonitor 시작
                          ├─ 실제 API 호출
                          ├─ 응답 시간 측정
                          │  └─ > 200ms? → 경고 로깅
                          ├─ CachingInterceptor 저장
                          └─ PerformanceMonitor 기록
                             ├─ Analytics 이벤트
                             ├─ p95/p99 계산
                             └─ 대시보드 업데이트
```

### 4. A/B 테스팅 흐름
```
App Start
    │
    ▼
Firebase Remote Config 가져오기
    │
    ├─ coaching_ui_variant: "control" / "treatment"
    ├─ onboarding_flow: "simple" / "advanced"
    └─ api_cache_duration: 3600
    │
    ▼
AnalyticsManager.init()
    │
    ├─ getUserVariant() → "treatment"
    ├─ setUserProperty("ab_test_variant", "treatment")
    └─ 모든 이벤트에 variant 포함
    │
    ▼
Firebase Analytics로 전송
    │
    ▼
BigQuery에서 분석
    │
    ├─ "control" 그룹: coaching_completion_rate = 45%
    └─ "treatment" 그룹: coaching_completion_rate = 60% → +15% 승리!
```

---

## 🔄 데이터 흐름 예제: 측정 완료

```
1. 사용자가 측정 완료 버튼 클릭
   └─ MeasurementProcessBloc.add(MeasurementCompleted())

2. HardwareSimulator.simulateADCRead()
   └─ 24-bit ADC 데이터 생성 (혈당값: 70-200)

3. MeasurementRepository.saveMeasurement()
   │
   ├─ 온라인?
   │  ├─ YES: POST /api/measurements
   │  │        ├─ CachingInterceptor 거쳐서 전송
   │  │        ├─ PerformanceMonitor (응답 시간 < 200ms?)
   │  │        ├─ 성공 → Hive 캐시
   │  │        └─ 실패 → Sync Queue 추가
   │  │
   │  └─ NO: Hive 로컬 저장 + Sync Queue 추가
   │
   ▼

4. AnalyticsManager.logMeasurementCompleted()
   ├─ Firebase Analytics로 전송
   │  └─ {measurementType, value, duration, variant}
   │
   └─ BigQuery에서 분석
      └─ "혈당 측정이 평균 30초 걸림"

5. HomeBloc.add(RefreshHealthScore())
   ├─ DataHubRepository.getTrendData()
   ├─ 캐시 확인 (1시간 이내?)
   │  ├─ YES: 캐시에서 즉시 반환
   │  └─ NO: API 호출 + 캐시 업데이트
   │
   └─ HomePage 위젯 업데이트

6. 동기화 (오프라인 상태라면)
   └─ OfflineModeManager._processSyncQueue()
      ├─ 5분 마다 자동 실행
      ├─ 배치: 100개 항목씩
      ├─ 충돌 해결: 로컬 타임스탬프 > 원격? → 로컬 승리
      └─ 성공 → Sync Queue에서 제거
```

---

## 🎯 MVP vs Full 비교

| 항목 | MVP (30경로) | Full (150+경로) | 개발시간 |
|------|-----------|-------------|--------|
| **홈** | ✅ | ✅ | 1주 |
| **측정** | ✅ | ✅ | 2주 |
| **데이터 허브** | ✅ | ✅ | 2주 |
| **코칭** | ✅ (규칙) | ✅ (규칙 + ML) | 2주 |
| **설정** | ✅ | ✅ | 1주 |
| **인증** | ✅ | ✅ | 1주 |
| **마켓플레이스** | ❌ | ✅ | 2주 |
| **원격진료** | ❌ | ✅ | 2주 |
| **커뮤니티** | ❌ | ✅ | 2주 |
| **백엔드 (3개)** | ✅ (3개) | ✅ (3개) | 3주 |
| **백엔드 (9개)** | ❌ | ✅ | 6주 |
| **펌웨어** | ✅ (시뮬) | ✅ | 4주 |
| **테스트** | △ (기본) | ✅ | 2주 |
| **배포** | ✅ | ✅ | 1주 |
| **총 시간** | **12주** | **24주** | - |

---

## 🚀 배포 및 확장 전략

### Phase 1 (MVP): 8-12주
```
✅ 30개 경로 활성화
✅ 3개 마이크로서비스 (mock)
✅ 오프라인-우선 동작
✅ A/B 테스팅 시작
✅ 앱 스토어 배포 (Beta)
└─ 목표: 1,000 사용자 (테스트군)
```

### Phase 2 (Expansion): 6-8주
```
🔄 마켓플레이스 활성화
🔄 원격진료 추가
🔄 ML 코칭 활성화 (72시간 예측)
🔄 9개 마이크로서비스 완전 구현
└─ 목표: 10,000 사용자
```

### Phase 3 (Scale): 4-6주
```
🔄 커뮤니티 기능 활성화
🔄 펌웨어 실제 하드웨어 연동
🔄 고급 분석 (BigQuery)
🔄 Kubernetes 배포
└─ 목표: 100,000 사용자
```

---

## 💾 데이터 영속성

### 로컬 (Hive)
```
offline_data:
  ├─ health_score_user1: {...}
  ├─ recent_measurements_user1: [...]
  ├─ trend_data_user1_glucose_7d: {...}
  └─ environment_info: {...}

sync_queue:
  ├─ {type: "measurement", data: {...}, timestamp: ...}
  ├─ {type: "coaching", data: {...}, timestamp: ...}
  └─ {type: "external_sync", data: {...}, timestamp: ...}

metadata:
  ├─ last_sync_time: 2024-01-15T10:30:00Z
  ├─ user_id: "user_123"
  └─ app_version: "1.0.0"
```

### 원격 (Mock/Firebase)
```
Firebase Firestore:
  users/
    └─ user_123/
       ├─ profile: {...}
       ├─ measurements/
       │  └─ measurement_001: {...}
       └─ health_score: {...}

Firebase Realtime Database (A/B 테스트):
  remote_config/
    └─ coaching_ui_variant: "treatment"
```

---

## 🔐 보안 계층

```
┌─────────────────────────────────┐
│ HTTPS Only (TLS 1.3)            │
└─────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ API Authentication              │
│  └─ JWT Token (Phase 2)        │
└─────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ Data Encryption                 │
│  ├─ Hive: AES-256 (optional)   │
│  └─ Network: TLS               │
└─────────────────────────────────┘
         │
         ▼
┌─────────────────────────────────┐
│ PII Protection                  │
│  ├─ No user ID in analytics    │
│  ├─ Data anonymization         │
│  └─ 90-day auto deletion       │
└─────────────────────────────────┘
```

---

## 📊 모니터링 대시보드

### 성능 대시보드 (Performance Dashboard Page)
- ✅ MVP 모드 상태
- ✅ 기능 플래그 활성화 상태
- ✅ 오프라인 동기화 대기 항목
- ✅ API 응답 시간 (avg, p95, p99)
- ✅ 캐시 히트율
- ✅ A/B 테스트 변형
- ✅ Analytics 이벤트 테스트

### Firebase Console
- ✅ Real-time Events
- ✅ Analytics Dashboard
- ✅ Remote Config
- ✅ Performance Monitoring
- ✅ Crashlytics (설정 예정)

### BigQuery 분석
```sql
-- 코칭 효과 (A/B 테스트)
SELECT variant, COUNT(*), AVG(coaching_completion_rate)
FROM analytics_events
WHERE event_name = 'coaching_completed'
GROUP BY variant;

-- API 성능
SELECT endpoint, COUNT(*), AVG(response_ms), PERCENTILE_CONT(response_ms, 0.95)
FROM analytics_events
WHERE event_name = 'api_response_time'
GROUP BY endpoint;

-- 사용자 리텐션
SELECT DATE(event_timestamp), COUNT(DISTINCT user_id)
FROM analytics_events
WHERE event_name = 'app_open'
GROUP BY DATE(event_timestamp);
```

---

## 🎓 학습 및 개선

### 지표 추적
- 📈 DAU/MAU (Daily/Monthly Active Users)
- 📈 리텐션율 (7일, 30일)
- 📈 코칭 완료율
- 📈 API 응답 시간 (p95 < 200ms 목표)
- 📈 오프라인 동기화 성공율

### 지속적 개선
1. **주간**: 성능 메트릭 검토
2. **월간**: 사용자 만족도 조사 (5점 척도)
3. **분기**: A/B 테스트 결과 분석 및 반영
4. **반기**: 기능 추가 우선순위 결정

---

## 🔗 문서 참조

- 📄 [MVP 설정](lib/config/mvp_config.dart) - 모든 구성 옵션
- 📄 [서비스 로케이터](lib/config/service_locator.dart) - DI 설정
- 📄 [Analytics](lib/services/analytics_manager.dart) - 이벤트 추적
- 📄 [오프라인](lib/services/offline_mode_manager.dart) - 동기화 로직
- 📄 [성능 대시보드](lib/presentation/screens/performance_dashboard_page.dart) - 모니터링
- 📄 [Firebase 설정](FIREBASE_SETUP.md) - Firebase 통합

---

**마지막 업데이트**: 2024년 1월
**상태**: MVP 개발 진행 중 (Phase 1)
**완성도**: 60-65%
